import { Paths } from "../../types";

function buildPage() {
  return `
  <div>
    <h1>Sign In Page
    </h1>
    <ypr-nav></ypr-nav>
  </div>
`
}

export function SignInPage() {
  return buildPage();
}
